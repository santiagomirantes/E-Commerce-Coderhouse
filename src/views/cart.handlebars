<h1>Products</h1>

<div id="products">

  {{#each products}}
    <div class="product">
      <p class="header">{{this.code}}</p>
      <h2>{{this.title}}</h2>
      <h3>{{this.price}}</h3>
      <p>{{this.description}}</p>
    </div>
  {{/each}}

</div>

<script src="/scripts/products.js"></script>